Sandwich : struct {
    /*
    * Now we are _within_ the sandwich...
    * Holy buckets!
    */
}

// Hello, Sailor!
main : () -> i32 {
    // Here is an exciting variable binding!
    let x = num() + ((10 + 2) * 2);
    x += 1 + 2;

    // Discarded!
    x + 1;

    // let z = 10; // ---> Compiler error!
    let y = 4;
    y = y - 2;

    x + y // or `return x + y;`
}

num : () -> i32 {
    1
}
