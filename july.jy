Sandwich : struct {
    /*
    * Now we are _within_ the sandwich ...
    * Holy buckets!
    */
}

// Hello, Sailor!
main : () -> i32 {
    // Here is an exciting variable binding!
    let x = num() + ((10 + 2) * 2);

    // This expression does nothing, and is discarded;
    x + 1;

    // let y = 10; // ---> Compiler error!
    let y: i32 = 10;

    x // `x` or `return x;`
}

num : () -> i32 {
    1
}
